import { Card, CardHeader, CardTitle } from '@/components/ui/card';

export default function TeacherDashboard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Tableau de Bord du Professeur</CardTitle>
      </CardHeader>
    </Card>
  );
}
